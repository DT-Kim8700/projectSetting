spring:
  datasource:
    # url
    url: jdbc:h2:tcp://localhost/~/schedule
    # db name
    username: sa
    # db password
    password:
    # db driver
    driver-class-name: org.h2.Driver

  jpa:
    # hibernate
    hibernate:
      ddl-auto: create
    generate-ddl: true
    open-in-view: true
    show-sql: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace
